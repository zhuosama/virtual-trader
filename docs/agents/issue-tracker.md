# Issue Tracker

Current tracker: in-repo Hxx task documents.

## Current Convention

- Task briefs live under `docs/hermes-hxx-...md`.
- Workflow slices live in `docs/agents/next-slices.md`.
- Active and closed strategy optimization items are tracked in `docs/strategy-optimization-sync.md`.
- GitHub remote exists (`zhuosama/virtual-trader`), but H43 did not create GitHub issues automatically.

## Future GitHub Convention

If GitHub Issues are enabled for this workflow later:

- Use one issue per vertical slice.
- Add enough acceptance criteria for an AFK agent to execute the issue.
- Keep strategy promotion decisions in repo docs even when issues exist.

