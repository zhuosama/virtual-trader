# H25 — Generate Real CSI300 Historical Universe via Qlib Collector

## Goal

Try to produce a real historical CSI300 universe file without a Tushare token by running Qlib's official CN index collector.

If successful:
- Generate `~/.qlib/qlib_data/cn_data/instruments/csi300.txt`
- Import it with `scripts/ingest_cn_pit_data.py --import-qlib-universe`
- Regenerate `data/cn_pit/validation_report.json`
- Rerun the value backtest gate

If unsuccessful:
- Do not overwrite `data/cn_pit/universe.jsonl`
- Keep validation `BLOCKED`
- Save a concise failure report with exact commands and errors

## References

- Qlib CN index collector README:
  - https://github.com/microsoft/qlib/blob/main/scripts/data_collector/cn_index/README.md
  - Command:
    ```bash
    python collector.py --index_name CSI300 --qlib_dir ~/.qlib/qlib_data/cn_data --method parse_instruments
    ```
- Qlib data docs:
  - https://qlib.readthedocs.io/en/v0.7.2/component/data.html

## Current Local State

- `TUSHARE_TOKEN` is not set.
- `tushare` is not installed.
- `qlib` is not installed in `/Users/zhuosama/.hermes/hermes-agent/venv`.
- Default Qlib instruments file is missing:
  - `/Users/zhuosama/.qlib/qlib_data/cn_data/instruments/csi300.txt`
- Current validation must remain `BLOCKED` until real historical universe evidence exists.

## Ownership

You may write:
- `~/.qlib/qlib_data/cn_data/instruments/csi300.txt` if generated by Qlib collector
- `data/cn_pit/universe.jsonl` only after successful real Qlib import
- `data/cn_pit/universe_snapshots.jsonl` only after successful real Qlib import
- `data/cn_pit/validation_report.json`
- `reports/h25_qlib_collector_report.md`

Avoid code changes unless the collector/import uncovers a real bug.

Do not revert unrelated files.

## Procedure

### 1. Check existing state

Run:

```bash
test -f ~/.qlib/qlib_data/cn_data/instruments/csi300.txt && echo FOUND || echo MISSING
/Users/zhuosama/.hermes/hermes-agent/venv/bin/python -c "import importlib.util; print('qlib', importlib.util.find_spec('qlib') is not None)"
```

### 2. Try official Qlib collector

Preferred approach:
- Use a temporary workspace, e.g. `/tmp/qlib-h25`
- Download or clone only what is needed from the official `microsoft/qlib` repo.
- Install only minimal requirements needed by `scripts/data_collector/cn_index/collector.py`.
- Run:

```bash
/Users/zhuosama/.hermes/hermes-agent/venv/bin/python collector.py \
  --index_name CSI300 \
  --qlib_dir /Users/zhuosama/.qlib/qlib_data/cn_data \
  --method parse_instruments
```

If dependencies or network fail, capture exact error.

### 3. Validate generated file

If `csi300.txt` exists:

```bash
wc -l ~/.qlib/qlib_data/cn_data/instruments/csi300.txt
head -5 ~/.qlib/qlib_data/cn_data/instruments/csi300.txt
tail -5 ~/.qlib/qlib_data/cn_data/instruments/csi300.txt
```

Sanity checks:
- expected columns: symbol, start_date, end_date
- nonzero rows
- multiple date ranges, not just a flat current snapshot

### 4. Import into virtual trader

Run:

```bash
cd /Users/zhuosama/.hermes/virtual-trader
/Users/zhuosama/.hermes/hermes-agent/venv/bin/python scripts/ingest_cn_pit_data.py \
  --import-qlib-universe \
  --qlib-dir /Users/zhuosama/.qlib/qlib_data/cn_data \
  --market csi300

/Users/zhuosama/.hermes/hermes-agent/venv/bin/python scripts/ingest_cn_pit_data.py --validate
```

### 5. Rerun gate smoke test

Run a small gate smoke:

```bash
cd /Users/zhuosama/.hermes/virtual-trader
/Users/zhuosama/.hermes/hermes-agent/venv/bin/python - <<'PY'
import sys, json
sys.path.insert(0, "backtest/experiments")
from fundamental_backtest import CN_PIT_FileSource, run_fundamental_backtest
src = CN_PIT_FileSource("data/cn_pit")
res = run_fundamental_backtest(
    src,
    start_date="2025-01-01",
    end_date="2026-05-18",
    capital=500000,
    top_n=8,
    max_position_pct=0.10,
    rebalance_freq_days=63,
)
print(json.dumps({
    "static_data_quality": src.data_quality.to_dict(),
    "period_data_quality": src.data_quality_for_period("2025-01-01", "2026-05-18").to_dict(),
    "can_deploy": res.can_deploy,
    "deploy_blockers": res.deploy_blockers,
    "metrics": {
        "total_return": res.metrics.get("total_return"),
        "sharpe_ratio": res.metrics.get("sharpe_ratio"),
        "trade_count": res.metrics.get("trade_count"),
    },
}, indent=2, ensure_ascii=False))
PY
```

### 6. Report

Write `reports/h25_qlib_collector_report.md` with:

- whether Qlib collector was obtained,
- exact commands run,
- whether `csi300.txt` was generated,
- row count and sample lines,
- whether import succeeded,
- validation status,
- backtest deploy gate result,
- next blocker if still blocked.

## Acceptance Criteria

Successful H25:
- `data/cn_pit/universe.jsonl` source provider becomes `qlib:instruments`
- `data/cn_pit/universe_snapshots.jsonl` exists
- `validation_report.json.status` is either:
  - `PASSED` if all blockers clear, or
  - `BLOCKED` with concrete non-survivorship blockers
- Backtest gate result is recorded

Unsuccessful H25:
- Existing `universe.jsonl` is not overwritten
- `validation_report.json.status` remains `BLOCKED`
- Failure report contains enough detail for the next attempt
