# Repo Hygiene Report — 2026-06-06

Handoff: handoffs/2026-06-06-mac-claude-code-to-hermes-virtual-trader-repo-hygiene.md
Executor: mac/hermes
Repo: ~/.hermes/virtual-trader (remote: github.com/zhuosama/virtual-trader.git)
Started: 2026-06-06 20:XX UTC+8

---

## Phase 0: Baseline Snapshot

```
git status --porcelain | wc -l:  325
git ls-files | wc -l:           299
git rev-parse HEAD:             14b6f47631d3a082a897d9e335b95440bc14c0f5
Branch:                         main
Ahead of origin/main:           60 commits
Merge/rebase in progress:       No
```

**Modified files (11):**
- .gitignore
- backtest/market_data.py
- docs/data-backfill-runbook.md
- reports/charts/cumulative_returns.png
- reports/charts/daily_pnl.png
- reports/charts/drawdown.png
- reports/charts/position_distribution.png
- strategies/performance_history.json
- tests/audit_layer/test_coordinator_audit_flow.py
- tests/audit_layer/test_market_data_provider.py
- tests/audit_layer/test_settlement_idempotent.py

**Untracked count:** 314

---

## Phase 1: Delete 3 Garbage Files

Dry-run (verify 0-byte files):

```bash
cd ~/.hermes/virtual-trader
wc -c -- '+6.2f}%' '2d}' '6.2f}'
```

Result: All 3 confirmed 0 bytes → `rm -- '+6.2f}%' '2d}' '6.2f}'` successful. Verified with `ls` — all gone.

---

## Phase 2: Expand .gitignore (Run Artifacts / Regenerable Data)

### Classification of grey-area untracked directories

#### agents/workflows/ — → IGNORE
- 32 untracked workflow JSON files (pre/post/wekly 2026-05-19 to 2026-06-05)
- Regenerated each pre/post-market cron run by coordinator
- **Decision: IGNORE** (auto-regenerated; old tracked ones in `git ls-files` stay but are legacy)

#### data/cn_pit/ (excluding raw/) — → MIXED: IGNORE
- `raw/` subdirs: already ignored (per-ticker Tushare cache)
- Generated files (prices.csv, fundamentals.jsonl etc.): **re-fetchable from Tushare API** using scripts
- Diagnostic JSON files (*_diagnostic.json): regenerable by scripts
- Coverage JSON files: regenerable
- **Decision: IGNORE all of data/cn_pit/** except what's already tracked (ohlcv_coverage_h53fix.json, ohlcv_h53fix_tushare_qfq.csv stay tracked as they are PIT quality baselines)
- Actually — wait, h53fix files are git-tracked and are NOT being reclassified. We're only adding ignore rules for **untracked** data. The existing tracked ones stay.
- **Decision: Add `data/cn_pit/` to .gitignore** (all untracked cn_pit artifacts are regenerable from Tushare)

#### backtest/runs/ — → IGNORE
- 24+ untracked JSON run files + markdown reports
- Regenerated on each backtest run
- **Decision: IGNORE** (reproducible; also large)

#### backtest/factors/ — → IGNORE
- gtja191 subdirectory + MANIFEST.sha256 + UPSTREAM_DIFF.md
- `unsupported.md` is documentation — but mixed with large binary factor cache
- **Decision: IGNORE** (factor cache is large and downloadable from upstream; unsupported.md can be moved if needed)

#### backtest/experiments/ — → TRACK
- Code files: fundamental_backtest.py, runner.py, value_variants.py, win_rate_variants.py
- These are source code, not run artifacts
- **Decision: TRACK** (add in Phase 4)

#### reports/ — → MIXED
- `reports/charts/`: generated PNGs → **IGNORE** (already per handoff)
- `reports/daily/`: daily markdown reports → **IGNORE** (regenerated by cron; already partially tracked legacy ones from Apr stay)
- Individual Hxx reports (h30_pit_value_backtest_report.md etc.): these are **research output, easy to re-render from scripts** → **IGNORE** (scripts that generate them exist, rerunnable)
- `reports/baseline_report.md`, `reports/final_comparison_report.md`, `reports/backtest_20260519.md`: these are generated outputs → **IGNORE**
- **Decision: IGNORE all reports/** except already-tracked legacy files

#### output/ — → IGNORE (run output, always regenerated)

#### logs/ — → IGNORE (runtime logs, always regenerated)

#### value_account/ — → MIXED
- account_manager.py: source code → **TRACK**
- h34_shadow_account_config.json: config file → **TRACK** (no sensitive data per quick scan)
- logs/, reports/: generated → **IGNORE**
- **Decision: TRACK code files, IGNORE generated dirs**

#### sec_edgar/ — → TRACK
- edgar_client.py: source code → **TRACK**
- cache/: regenerable → **IGNORE**
- **Decision: TRACK client code**

#### .hermes/ — → IGNORE (Hermes runtime state, plans subdir)

#### docs/agents/ — → TRACK (agent workflow documentation)
#### docs/drafts/ — → TRACK (drafts are documentation in progress)
#### docs/spikes/ — → TRACK (research spike reports)
#### docs/superpowers/ — → TRACK (design docs, plans, specs)
#### All standalone doc files → TRACK

### Expanded .gitignore rules

```
# === New rules from repo-hygiene-2026-06-06 ===
...
```

Applied to .gitignore — see diff in commit. Verified: untracked count dropped from **314 → 139**.

---

## Phase 3: Stop tracking tracked run artifacts (git rm --cached)

**Dry-run:** `git ls-files reports/charts/` confirmed 4 PNGs tracked.

**Executed:**
```
git rm --cached reports/charts/cumulative_returns.png
git rm --cached reports/charts/daily_pnl.png
git rm --cached reports/charts/drawdown.png
git rm --cached reports/charts/position_distribution.png
```

**Result:** All 4 removed from index, preserved on disk. New .gitignore rule `reports/charts/` prevents re-tracking.

Not removing tracked legacy reports/daily/ (45 files), reports/weekly/ (4), backtest/runs/ (6 legacy h53fix runs) — they are historical committed artifacts. The .gitignore prevents new auto-generated ones from being tracked.

---

## Phase 4: git add real assets (source code / docs / tests / data)

### Dry-run (git add -n) organized by category

